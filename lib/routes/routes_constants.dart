class RoutesConstants {
  // Định nghĩa hằng số `splashScreenRoute` với giá trị là "/".
  // Đây là route mặc định, thường được sử dụng để điều hướng đến màn hình splash (màn hình khởi động) của ứng dụng.
  static const String splashScreenRoute = "/";

  // Định nghĩa hằng số `landingRoute` với giá trị là "/landing".
  // Route này được sử dụng để điều hướng đến màn hình landing (màn hình chính hoặc màn hình sau khi khởi động).
  static const String landingRoute = "/landing";

  // Định nghĩa hằng số `noInternetRoute` với giá trị là "/noInternet".
  // Route này được sử dụng để điều hướng đến trang thông báo không có kết nối Internet.
  static const String noInternetRoute = "/noInternet";
}
